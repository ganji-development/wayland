name: 'PR Checks'

# Consolidated PR-gate workflow for the Wayland fork.
#
# This file is the union of the legacy upstream `pr-checks.yml` and the
# Wayland-fork `pr-gate.yml` (commit dc59d43e3). The two duplicated
# tsc/lint/vitest/build steps across separate runners; they are now one
# workflow.
#
# Job names ("Code Quality", "Unit Tests (<os>)", "Coverage Test",
# "I18n Check", "Security Audit E2E", "Release Script Test") are
# referenced by branch protection required-checks and by
# `pr-checks-docs.yml` (the docs-only stub that mirrors required-check
# names so docs-only PRs can satisfy the same set). DO NOT rename
# without updating both branch-protection settings and pr-checks-docs.yml.
#
# Cross-platform PACKAGED builds (electron-builder, ~45min per platform)
# were removed from PRs -- they now live in `build-matrix.yml`, gated on
# the repo variable `CI_BUILD_MATRIX_ENABLED`. PRs still run a fast
# unpackaged `electron-vite build` to catch compile errors.
#
# Required repo secrets / variables:
#   secrets.CODECOV_TOKEN          -- optional, falls back to OIDC if unset
#                                     (only used on FerroxLabs/wayland)
#   vars.CI_BUILD_MATRIX_ENABLED   -- owned by build-matrix.yml, not used here
#
# Security: no untrusted PR/issue text is interpolated into any run: step.
# env.PR_BASE_REF is set by ./.github/actions/checkout-pr from trusted
# sources (github.base_ref or the PR's base.ref via gh api).

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to run checks on'
        required: true
        type: string
      skip_build_test:
        description: 'Skip the unpackaged build / e2e job'
        type: boolean
        required: false
        default: false
  pull_request:
    types: [opened, synchronize, edited, closed, ready_for_review, reopened]
    branches: [main, dev, 'feat/**', 'release/**', 'integration/**']
    # NO paths-ignore. This workflow is the ONLY owner of the required check
    # names (`Code Quality`, `Unit Tests (<os>)`), so it must run on every PR —
    # including docs-only ones — or those checks never report at all.
    #
    # Docs-only PRs are handled INSIDE the workflow: the `changed-files` job below
    # classifies the diff and the expensive jobs skip themselves, while the
    # aggregators still stamp a real verdict. Splitting that decision into a
    # second workflow is what produced the #925 gate bypass (a stub workflow
    # publishing green under these same names on mixed PRs). Do not reintroduce it.

concurrency:
  group: pr-checks-${{ github.event.pull_request.number || inputs.pr_number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  issues: read
  checks: write

env:
  BUN_INSTALL_REGISTRY: 'https://registry.npmjs.org/'
  # Wayland-core release stream does not exist yet (see commit 48d155826).
  WCORE_SKIP: '1'
  CI: 'true'
  NODE_OPTIONS: '--max-old-space-size=8192'
  # #940: four @wayland MCP connectors (apple, imap, news, cal.com) build from
  # the sibling waylandmcp repo, which has no GitHub remote yet - CI cannot
  # check it out. scripts/build-mcp-servers.js now FAILS the build when a
  # connector cannot be bundled, so CI must opt out explicitly until that repo
  # lands. DELETE this line the moment waylandmcp is checkout-able in CI; that
  # is the whole point of the gate.
  WAYLAND_ALLOW_MISSING_MCP: '1'

jobs:
  # Cancel in-progress runs when PR is closed (merged or manually closed)
  cancel-if-closed:
    name: Cancel if PR closed
    if: github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR closed, cancelling in-progress runs via concurrency."

  # Job 0: classify the diff. Owns the docs-only decision for every job below.
  # Deliberately NOT a required check — it is an input to the gate, not the gate.
  changed-files:
    name: Classify Changed Files
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      docs_only: ${{ steps.classify.outputs.docs_only }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Classify changed paths
        id: classify
        env:
          # SHAs only, via env — never interpolated into the shell.
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ -z "$BASE_SHA" ] || [ -z "$HEAD_SHA" ]; then
            # workflow_dispatch has no PR payload. Fail closed: run everything.
            echo "No PR diff context (manual dispatch); treating as NOT docs-only."
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          node scripts/docs-only-changes.mjs --base "$BASE_SHA" --head "$HEAD_SHA"

  # Job 1: Code quality checks (TypeScript, Oxlint, Oxfmt via prek)
  code-quality:
    name: Code Quality
    # `always()` and the absence of `needs:` are both load-bearing. This is a
    # required check, and GitHub counts a SKIPPED required check as a PASS — so a
    # skipped `Code Quality` reports green. It must therefore always run and
    # always reach a step that stamps a real verdict, including on drafts.
    if: always() && github.event.action != 'closed'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      # Fails in ~20s of runner time rather than reporting green-by-skip.
      - name: Refuse to report green on a draft PR
        if: github.event_name != 'workflow_dispatch' && github.event.pull_request.draft == true
        run: |
          echo "::error::Draft PR - the quality gate did NOT run. This required check is red on purpose: it must never report green when the checks it gates did not execute. Mark the PR ready for review to run it."
          exit 1

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch the Constitution provenance producer commit
        shell: bash
        run: |
          # Same reason as the unit-tests job: this job's "Prove Constitution
          # filesystem authority" step runs constitutionFsService, which
          # re-derives the 991c502 producer corpus from git. That commit is
          # reachable from no remote ref, so it must be fetched by SHA.
          SHA=$(node -p "require('./tests/fixtures/constitution-fs/base-991c502-committed/fixture-manifest.json').producerCommit")
          echo "producer commit: $SHA"
          for attempt in 1 2 3; do
            if git fetch --depth=1 --no-tags origin "$SHA"; then exit 0; fi
            echo "fetch attempt $attempt failed; retrying" >&2
            sleep 5
          done
          echo "could not fetch producer commit $SHA" >&2
          exit 1

      # Classified inline rather than via `needs: changed-files`, because a
      # failing dependency would SKIP this job, and a skipped required check
      # reads as a pass.
      - name: Classify changed paths
        id: classify
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ -z "$BASE_SHA" ] || [ -z "$HEAD_SHA" ]; then
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          node scripts/docs-only-changes.mjs --base "$BASE_SHA" --head "$HEAD_SHA"

      - name: Resolve PR context
        uses: ./.github/actions/checkout-pr
        with:
          pr_number: ${{ inputs.pr_number }}
          github_token: ${{ github.token }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
          cache: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run postinstall
        run: npm run postinstall || true

      - name: Install prek
        run: npm install -g @j178/prek

      - name: Run prek checks
        env:
          PR_BASE_REF: ${{ env.PR_BASE_REF }}
        run: prek run --from-ref "origin/$PR_BASE_REF" --to-ref HEAD

      # Skipped on docs-only PRs: no shipped code changed, so there is no new
      # claim to prove. Skipping a STEP is safe — the job still stamps its own
      # real conclusion, unlike a skipped job.
      - name: Prove Constitution filesystem authority from a cold checkout
        if: steps.classify.outputs.docs_only != 'true'
        run: |
          cargo test --locked --manifest-path native/constitution-fs/Cargo.toml
          node scripts/prepareConstitutionFs.js
          bunx vitest run tests/unit/process/services/constitution/constitutionFsTransaction.test.ts tests/unit/process/services/constitution/constitutionFsService.test.ts tests/unit/verifyPackagedResources.test.ts

  # Job 2: Unit tests across all platforms.
  # NOTE: job name "Unit Tests (<os>)" is a required check, and THIS WORKFLOW IS
  # ITS ONLY OWNER. There used to be a pr-checks-docs.yml mirroring these names
  # with echo jobs; it was deleted because on a mixed PR it published green under
  # the required names while the real shards failed (#925). Docs-only PRs are
  # handled by the `changed-files` job instead. Do not rename or collapse.
  # Sharded worker jobs: the full vitest suite exceeds the 10-min job timeout on
  # a single runner (native better-sqlite3 + timer-based channel-plugin tests),
  # so split it across 4 file-shards per OS that each finish well under the cap.
  # The required check names "Unit Tests (<os>)" are preserved by the aggregator
  # jobs below (so branch protection + pr-checks-docs.yml need no changes).
  unit-tests:
    name: Unit Tests Shard (${{ matrix.os }} ${{ matrix.shard }}/4)
    # Safe to skip these (they are NOT required checks) — the aggregators below
    # are, and they convert "shards skipped" into a real verdict.
    needs: changed-files
    if: |
      needs.changed-files.outputs.docs_only != 'true' &&
      (github.event_name == 'workflow_dispatch' ||
       (github.event.action != 'closed' && github.event.pull_request.draft == false))
    runs-on: ${{ matrix.os }}
    # #358: shard 1/4 was ~19m on ubuntu (vs the ~3.5m average) from real-timer
    # error-path tests + the skillRecall BM25 full-corpus gate. Those are now
    # fixed — the BM25 gate is gated out of PR shards behind RUN_RELEASE_GATES
    # (runs in release-gates.yml), and the iMessage delivery-poll / ConnectionTester
    # retry / killChild escalation tests no longer wait real seconds. Post-fix CI
    # on this PR: ubuntu/macos shards ~2min, windows shards 7.6-9.3min (windows
    # runners are just slow; tracked separately). The 35m stopgap drops to 15m —
    # ~1.6x over the slowest (windows, 9.3min). If windows variance trips the cap,
    # raise toward 18-20m.
    #
    # 2026-08-15: it tripped. windows-2022 shard 3/4 was cancelled at exactly
    # 15m00s on an otherwise green run - 11 of 12 shards passed, and a cancelled
    # shard reds out all three "Unit Tests (<os>)" required checks just like a
    # failure would. That shard carries verifyPackagedResources (63 cases that
    # each build a packaged fixture tree), so it is the slowest of the four.
    # Taking the comment's own advice rather than inventing a new number.
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-2022]
        shard: [1, 2, 3, 4]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # constitutionFsService authenticates the immutable 991c502 producer
          # corpus by re-deriving it from git history (git archive/show/read-tree).
          # That commit is 538 back, so a default depth-1 checkout made those tests
          # fail in CI while passing locally on a full clone. The test is right to
          # demand real history - it is a supply-chain authenticity check - so
          # deepen the checkout rather than weaken the assertion.
          fetch-depth: 0

      - name: Fetch the Constitution provenance producer commit
        shell: bash
        run: |
          # constitutionFsService re-derives the 991c502 producer corpus straight
          # from git (rev-parse / archive / show / read-tree). That commit is
          # reachable from NO remote ref - not main, not any PR branch, and the
          # two local tags holding it were never pushed - so `fetch-depth: 0`
          # cannot reach it either. The assertion passed only on long-lived
          # clones that still had the loose object, which is why it went unseen
          # until this branch first opened a PR.
          #
          # Fetch it by SHA, which GitHub permits; depth-1 carries the trees and
          # blobs the assertion needs. Deliberately a CI step rather than a
          # change to the test: it keeps network I/O out of the unit suite and
          # leaves the supply-chain assertion demanding real history.
          SHA=$(node -p "require('./tests/fixtures/constitution-fs/base-991c502-committed/fixture-manifest.json').producerCommit")
          echo "producer commit: $SHA"
          for attempt in 1 2 3; do
            if git fetch --depth=1 --no-tags origin "$SHA"; then exit 0; fi
            echo "fetch attempt $attempt failed; retrying" >&2
            sleep 5
          done
          echo "could not fetch producer commit $SHA" >&2
          exit 1

      - name: Resolve PR context
        uses: ./.github/actions/checkout-pr
        with:
          pr_number: ${{ inputs.pr_number }}
          github_token: ${{ github.token }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
          cache: true

      - name: Disable Windows Defender (Windows only)
        if: matrix.os == 'windows-2022'
        shell: pwsh
        run: |
          Set-MpPreference -DisableRealtimeMonitoring $true
          Add-MpPreference -ExclusionPath "${{ github.workspace }}"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run postinstall
        run: npm run postinstall || true

      - name: Run unit tests (vitest shard)
        run: bunx vitest run --shard=${{ matrix.shard }}/4

      # Bun-native suites import bun:test and cannot be collected by Vitest.
      # Run the complete deterministic corpus once per OS; the required unit
      # test aggregator below therefore fails if either runner is red.
      - name: Run Bun-native tests
        if: matrix.shard == 1
        run: bun run test:bun

  # Aggregator jobs preserve the required-check names "Unit Tests (<os>)".
  # Each waits on the full sharded matrix and fails if any shard (on any OS)
  # failed, so a single failing unit test anywhere blocks the merge. GitHub's
  # `needs.<job>.result` for a matrix job is success only when every cell
  # succeeded.
  #
  # The `always()` guard is LOAD-BEARING — do NOT "fix" it to `!cancelled()`.
  # It is tempting, because on a run cancelled by the concurrency group above
  # `needs.unit-tests.result` is "cancelled", which trips the != "success" check
  # and stamps a red required check from a dead run (three of them, so it reads as
  # "unit tests failing on all three platforms" when nothing is broken at all).
  # But `!cancelled()` would SKIP these jobs on a cancelled run, and GitHub counts
  # a skipped required check as a PASS. Since the live run publishes no
  # "Unit Tests (<os>)" check until all 12 shards finish (~10-20 min), the PR would
  # go CLEAN — and auto-merge would fire — while the real tests were still running.
  # That trades a cosmetic false-red for a fail-open merge gate. Never do it.
  #
  # So the red stays (it is fail-closed and correct), and the step below instead
  # makes the superseded case unmistakable in the log + job summary. The stale red
  # is transient: the newest run stamps these same check names and supersedes it.
  #
  # KNOWN LIMITATION (follow-up): each aggregator gates on the WHOLE 12-cell matrix,
  # so one macOS-only shard failure also reds out "Unit Tests (ubuntu-latest)" and
  # "Unit Tests (windows-2022)". Per-OS gating needs the matrix restructured.
  unit-tests-ubuntu:
    name: Unit Tests (ubuntu-latest)
    # The draft gate was REMOVED from this `if:` on purpose. It used to skip these
    # jobs on a draft PR, and GitHub counts a skipped required check as a PASS —
    # so every draft showed three green "Unit Tests (<os>)" checks while nothing
    # ran. These jobs now always run and always stamp a real verdict; the draft
    # case is handled in the step below.
    if: always() && github.event.action != 'closed'
    needs: [changed-files, unit-tests]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all unit-test shards passed
        env:
          SHARDS_RESULT: ${{ needs.unit-tests.result }}
          CLASSIFY_RESULT: ${{ needs.changed-files.result }}
          DOCS_ONLY: ${{ needs.changed-files.outputs.docs_only }}
          IS_DRAFT: ${{ github.event.pull_request.draft }}
        run: |
          if [ "$SHARDS_RESULT" = "success" ]; then
            echo "All unit-test shards passed."
            exit 0
          fi

          # Checked BEFORE the docs-only pass so a draft never reports green,
          # matching the Code Quality job.
          if [ "$IS_DRAFT" = "true" ] && [ "$SHARDS_RESULT" = "skipped" ]; then
            echo "::error::Draft PR - the unit tests did NOT run. This required check is red on purpose: it must never report green when the tests it gates did not execute. Mark the PR ready for review to run them."
            exit 1
          fi

          # The ONLY legitimate green-without-running case: the diff provably
          # touches nothing that can change runtime behaviour. Authority is
          # scripts/docs-only-changes.mjs, exercised by
          # tests/unit/scripts/docsOnlyChanges.test.ts.
          if [ "$DOCS_ONLY" = "true" ] && [ "$SHARDS_RESULT" = "skipped" ]; then
            echo "Docs-only PR: no shipped code changed, so the unit shards did not run."
            exit 0
          fi

          if [ "$CLASSIFY_RESULT" != "success" ]; then
            echo "::error::Could not classify the diff (Classify Changed Files: $CLASSIFY_RESULT), so this gate cannot prove the PR is docs-only. Failing closed."
            exit 1
          fi

          if [ "$SHARDS_RESULT" = "cancelled" ]; then
            echo "::warning::SUPERSEDED RUN - NOT A REAL TEST FAILURE. The shards were cancelled because a newer run replaced this one for the same PR. The newest run is authoritative."
            {
              echo "### Superseded run - NOT a real test failure"
              echo ""
              echo 'The unit-test shards were `cancelled`: a newer run replaced this one for the'
              echo 'same PR (the `pr-checks-<pr>` concurrency group is `cancel-in-progress`).'
              echo ""
              echo 'This job still fails **on purpose** - a required check must never report green'
              echo 'when the tests it gates did not actually run. The red is correct but STALE: the'
              echo 'newest run stamps its own verdict on these same check names and supersedes it.'
              echo ""
              echo '**Do not go bug-hunting.** Open the newest `PR Checks` run for this commit.'
            } >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          echo "One or more unit-test shards did not pass (result: $SHARDS_RESULT)."
          exit 1

  unit-tests-macos:
    name: Unit Tests (macos-14)
    # The draft gate was REMOVED from this `if:` on purpose. It used to skip these
    # jobs on a draft PR, and GitHub counts a skipped required check as a PASS —
    # so every draft showed three green "Unit Tests (<os>)" checks while nothing
    # ran. These jobs now always run and always stamp a real verdict; the draft
    # case is handled in the step below.
    if: always() && github.event.action != 'closed'
    needs: [changed-files, unit-tests]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all unit-test shards passed
        env:
          SHARDS_RESULT: ${{ needs.unit-tests.result }}
          CLASSIFY_RESULT: ${{ needs.changed-files.result }}
          DOCS_ONLY: ${{ needs.changed-files.outputs.docs_only }}
          IS_DRAFT: ${{ github.event.pull_request.draft }}
        run: |
          if [ "$SHARDS_RESULT" = "success" ]; then
            echo "All unit-test shards passed."
            exit 0
          fi

          # Checked BEFORE the docs-only pass so a draft never reports green,
          # matching the Code Quality job.
          if [ "$IS_DRAFT" = "true" ] && [ "$SHARDS_RESULT" = "skipped" ]; then
            echo "::error::Draft PR - the unit tests did NOT run. This required check is red on purpose: it must never report green when the tests it gates did not execute. Mark the PR ready for review to run them."
            exit 1
          fi

          # The ONLY legitimate green-without-running case: the diff provably
          # touches nothing that can change runtime behaviour. Authority is
          # scripts/docs-only-changes.mjs, exercised by
          # tests/unit/scripts/docsOnlyChanges.test.ts.
          if [ "$DOCS_ONLY" = "true" ] && [ "$SHARDS_RESULT" = "skipped" ]; then
            echo "Docs-only PR: no shipped code changed, so the unit shards did not run."
            exit 0
          fi

          if [ "$CLASSIFY_RESULT" != "success" ]; then
            echo "::error::Could not classify the diff (Classify Changed Files: $CLASSIFY_RESULT), so this gate cannot prove the PR is docs-only. Failing closed."
            exit 1
          fi

          if [ "$SHARDS_RESULT" = "cancelled" ]; then
            echo "::warning::SUPERSEDED RUN - NOT A REAL TEST FAILURE. The shards were cancelled because a newer run replaced this one for the same PR. The newest run is authoritative."
            {
              echo "### Superseded run - NOT a real test failure"
              echo ""
              echo 'The unit-test shards were `cancelled`: a newer run replaced this one for the'
              echo 'same PR (the `pr-checks-<pr>` concurrency group is `cancel-in-progress`).'
              echo ""
              echo 'This job still fails **on purpose** - a required check must never report green'
              echo 'when the tests it gates did not actually run. The red is correct but STALE: the'
              echo 'newest run stamps its own verdict on these same check names and supersedes it.'
              echo ""
              echo '**Do not go bug-hunting.** Open the newest `PR Checks` run for this commit.'
            } >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          echo "One or more unit-test shards did not pass (result: $SHARDS_RESULT)."
          exit 1

  unit-tests-windows:
    name: Unit Tests (windows-2022)
    # The draft gate was REMOVED from this `if:` on purpose. It used to skip these
    # jobs on a draft PR, and GitHub counts a skipped required check as a PASS —
    # so every draft showed three green "Unit Tests (<os>)" checks while nothing
    # ran. These jobs now always run and always stamp a real verdict; the draft
    # case is handled in the step below.
    if: always() && github.event.action != 'closed'
    needs: [changed-files, unit-tests]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all unit-test shards passed
        env:
          SHARDS_RESULT: ${{ needs.unit-tests.result }}
          CLASSIFY_RESULT: ${{ needs.changed-files.result }}
          DOCS_ONLY: ${{ needs.changed-files.outputs.docs_only }}
          IS_DRAFT: ${{ github.event.pull_request.draft }}
        run: |
          if [ "$SHARDS_RESULT" = "success" ]; then
            echo "All unit-test shards passed."
            exit 0
          fi

          # Checked BEFORE the docs-only pass so a draft never reports green,
          # matching the Code Quality job.
          if [ "$IS_DRAFT" = "true" ] && [ "$SHARDS_RESULT" = "skipped" ]; then
            echo "::error::Draft PR - the unit tests did NOT run. This required check is red on purpose: it must never report green when the tests it gates did not execute. Mark the PR ready for review to run them."
            exit 1
          fi

          # The ONLY legitimate green-without-running case: the diff provably
          # touches nothing that can change runtime behaviour. Authority is
          # scripts/docs-only-changes.mjs, exercised by
          # tests/unit/scripts/docsOnlyChanges.test.ts.
          if [ "$DOCS_ONLY" = "true" ] && [ "$SHARDS_RESULT" = "skipped" ]; then
            echo "Docs-only PR: no shipped code changed, so the unit shards did not run."
            exit 0
          fi

          if [ "$CLASSIFY_RESULT" != "success" ]; then
            echo "::error::Could not classify the diff (Classify Changed Files: $CLASSIFY_RESULT), so this gate cannot prove the PR is docs-only. Failing closed."
            exit 1
          fi

          if [ "$SHARDS_RESULT" = "cancelled" ]; then
            echo "::warning::SUPERSEDED RUN - NOT A REAL TEST FAILURE. The shards were cancelled because a newer run replaced this one for the same PR. The newest run is authoritative."
            {
              echo "### Superseded run - NOT a real test failure"
              echo ""
              echo 'The unit-test shards were `cancelled`: a newer run replaced this one for the'
              echo 'same PR (the `pr-checks-<pr>` concurrency group is `cancel-in-progress`).'
              echo ""
              echo 'This job still fails **on purpose** - a required check must never report green'
              echo 'when the tests it gates did not actually run. The red is correct but STALE: the'
              echo 'newest run stamps its own verdict on these same check names and supersedes it.'
              echo ""
              echo '**Do not go bug-hunting.** Open the newest `PR Checks` run for this commit.'
            } >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          echo "One or more unit-test shards did not pass (result: $SHARDS_RESULT)."
          exit 1

  # Job 3: Coverage test (Linux only) + Codecov upload
  coverage-tests:
    name: Coverage Test
    needs: changed-files
    if: |
      needs.changed-files.outputs.docs_only != 'true' &&
      (github.event_name == 'workflow_dispatch' ||
       (github.event.action != 'closed' && github.event.pull_request.draft == false))
    runs-on: ubuntu-latest
    # The coverage run executes the whole suite WITH instrumentation, so it needs
    # more than the 10-min unit-test cap. The test step is continue-on-error, so
    # the job passes as long as it finishes (a timeout is its only red signal).
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # Same git-history requirement as the unit-test shards.
          fetch-depth: 0

      - name: Fetch the Constitution provenance producer commit
        shell: bash
        run: |
          # constitutionFsService re-derives the 991c502 producer corpus straight
          # from git (rev-parse / archive / show / read-tree). That commit is
          # reachable from NO remote ref - not main, not any PR branch, and the
          # two local tags holding it were never pushed - so `fetch-depth: 0`
          # cannot reach it either. The assertion passed only on long-lived
          # clones that still had the loose object, which is why it went unseen
          # until this branch first opened a PR.
          #
          # Fetch it by SHA, which GitHub permits; depth-1 carries the trees and
          # blobs the assertion needs. Deliberately a CI step rather than a
          # change to the test: it keeps network I/O out of the unit suite and
          # leaves the supply-chain assertion demanding real history.
          SHA=$(node -p "require('./tests/fixtures/constitution-fs/base-991c502-committed/fixture-manifest.json').producerCommit")
          echo "producer commit: $SHA"
          for attempt in 1 2 3; do
            if git fetch --depth=1 --no-tags origin "$SHA"; then exit 0; fi
            echo "fetch attempt $attempt failed; retrying" >&2
            sleep 5
          done
          echo "could not fetch producer commit $SHA" >&2
          exit 1

      - name: Resolve PR context
        uses: ./.github/actions/checkout-pr
        with:
          pr_number: ${{ inputs.pr_number }}
          github_token: ${{ github.token }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
          cache: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run postinstall
        run: npm run postinstall || true

      - name: Run coverage tests
        id: coverage
        continue-on-error: true
        run: bun run test:coverage

      - name: Upload coverage to Codecov (Linux coverage only)
        if: always() && hashFiles('coverage/lcov.info') != '' && github.repository == 'FerroxLabs/wayland'
        uses: codecov/codecov-action@v5
        with:
          # No `use_oidc`. It defaults to false, and it must stay false here: this job
          # runs PR-authored code (bun install, postinstall, test:coverage), so an OIDC
          # token minted in it would let any PR author assert this repo's identity to
          # every relying party that trusts it. trustRootJobSeparation.test.ts pins that
          # invariant for the test jobs; this step is the same class.
          # `token` stays because it is the supported path once a CODECOV_TOKEN secret
          # exists; until then it resolves to '' and the upload goes tokenless.
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage/lcov.info
          fail_ci_if_error: false
          verbose: true

      - name: Skip Codecov upload when preconditions are not met
        if: always() && hashFiles('coverage/lcov.info') != '' && github.repository != 'FerroxLabs/wayland'
        shell: bash
        env:
          REPO: ${{ github.repository }}
        run: |
          echo 'Skipping Codecov upload due to unmet preconditions.'
          echo "Repository: $REPO"

      - name: Coverage result summary
        if: always()
        shell: bash
        env:
          COVERAGE_OUTCOME: ${{ steps.coverage.outcome }}
        run: |
          if [ "$COVERAGE_OUTCOME" = "failure" ]; then
            echo "::warning::Coverage tests failed (non-blocking). Check logs for failed test details."
            echo "## Coverage check (non-blocking warning)" >> $GITHUB_STEP_SUMMARY
            echo "Coverage command failed in this run. Please review test failures in logs." >> $GITHUB_STEP_SUMMARY
            if [ -f coverage/lcov.info ]; then
              echo "lcov.info exists, Codecov upload can still run." >> $GITHUB_STEP_SUMMARY
            else
              echo "lcov.info not found, Codecov upload was skipped." >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "## Coverage check" >> $GITHUB_STEP_SUMMARY
            echo "Coverage command passed." >> $GITHUB_STEP_SUMMARY
            if [ -f coverage/lcov.info ]; then
              echo "Uploaded Linux (ubuntu-latest) coverage to Codecov." >> $GITHUB_STEP_SUMMARY
            else
              echo "Coverage passed but lcov.info is missing; Codecov upload was skipped." >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Upload coverage artifacts
        if: always() && hashFiles('coverage/lcov.info') != ''
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          if-no-files-found: warn

  # Job 4: I18n validation (warning-only)
  i18n-check:
    name: I18n Check
    needs: changed-files
    if: |
      needs.changed-files.outputs.docs_only != 'true' &&
      (github.event_name == 'workflow_dispatch' ||
       (github.event.action != 'closed' && github.event.pull_request.draft == false))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
          cache: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run postinstall
        run: npm run postinstall || true

      - name: Run i18n validation (warning-only)
        id: i18n
        shell: bash
        run: |
          set -o pipefail
          node scripts/check-i18n.js 2>&1 | tee i18n-check.log

      - name: Publish i18n summary
        if: always()
        shell: bash
        run: |
          echo "## i18n validation" >> $GITHUB_STEP_SUMMARY
          if grep -q "warning" i18n-check.log; then
            echo "Missing/incomplete translations found. Please review warnings below." >> $GITHUB_STEP_SUMMARY
          else
            echo "No i18n warnings detected." >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "<details><summary>i18n log</summary>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```text' >> $GITHUB_STEP_SUMMARY
          cat i18n-check.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

  # Job 5: Fast unpackaged build + security-audit e2e spec.
  # Merged from pr-gate.yml (commit dc59d43e3). The HEAVY cross-platform
  # electron-builder packaging step that used to live in pr-checks.yml
  # was removed -- it now lives in build-matrix.yml (push/cron + flag).
  security-audit:
    name: Security Audit E2E
    needs: changed-files
    if: |
      needs.changed-files.outputs.docs_only != 'true' &&
      github.event.action != 'closed' &&
      github.event.pull_request.draft == false &&
      inputs.skip_build_test != true
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # Pinned to ../mise.toml (node = "24"). engines.node allows >=22 <25.
          node-version: '24'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          # Pinned to ../mise.toml (bun = "1.3").
          bun-version: '1.3'

      - name: Cache Bun + node_modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: pr-checks-bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            pr-checks-bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Postinstall (CI uses prebuilt binaries; see scripts/postinstall.js)
        run: bun run postinstall || true

      - name: Typecheck
        run: bunx tsc --noEmit

      - name: Lint
        run: bun run lint

      - name: Build (electron-vite, no packaging)
        run: bunx electron-vite build

      # The e2e spec launches the real Electron binary via _electron.launch().
      # The restored node_modules cache has electron without a usable binary
      # (path.txt missing / dist incomplete), and bun's --frozen-lockfile install
      # doesn't re-run electron's download script for the already-present package.
      # Remedy: explicitly run electron's (trusted) install.js, asserting a
      # COMPLETE dist (path.txt + the executable it names) so a bad install fails
      # HERE (with `set -x` diagnostics) instead of mid-spec.
      #
      # #361: since ~2026-06-26 an environmental egress cap on this runner
      # truncates the ~100MB binary zip to ~678KB (a lone v8_context_snapshot.bin)
      # on EVERY external host, so no download path can recover it. The durable
      # fix is to cache the extracted binary in GitHub infra (restore is NOT
      # subject to the external egress cap) and make the install idempotent so a
      # cache-warm binary is reused instead of nuked + re-downloaded every run.
      - name: Restore Electron binary cache
        id: electron-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            node_modules/electron/dist
            node_modules/electron/path.txt
          # Keyed on the resolved electron version (node_modules/electron exists
          # post-install); a version bump invalidates the entry.
          key: electron-bin-${{ runner.os }}-${{ hashFiles('node_modules/electron/package.json') }}

      - name: Ensure Electron binary is installed
        # ROOT CAUSE (#361): "Ensure Electron binary is installed" has failed
        # deterministically since ~2026-06-26 — dist/ ends up with only
        # v8_context_snapshot.bin and no path.txt, identically whether
        # ELECTRON_MIRROR points at npmmirror or the default github host. So it
        # is NOT a host-specific egress cap (that hypothesis was tested and
        # disproven on this PR). @electron/get's own failure is swallowed by a
        # console.error()+process.exit() flush race, hiding whether the cause is
        # a network truncation or extraction (disk).
        #
        # This step bypasses @electron/get: download the zip with curl (a client
        # not subject to whatever truncates @electron/get) with retries, then
        # HARD-ASSERT size + sha256 against electron's bundled checksums.json so a
        # truncated download fails loudly with the real byte count instead of
        # silently. Unzip into dist/ ourselves, with df before/after to surface
        # any ENOSPC. npmmirror first, default github as fallback. A complete
        # install here also finally lets the actions/cache Save warm for reuse.
        env:
          npm_config_disturl: 'https://electronjs.org/headers'
        run: |
          set -x
          df -h /
          electron_ok() {
            [ -s node_modules/electron/path.txt ] \
              && [ -x "node_modules/electron/dist/$(cat node_modules/electron/path.txt)" ]
          }
          if electron_ok; then
            echo "electron already present (cache-warm); skipping download"
            exit 0
          fi
          VER="$(node -p "require('./node_modules/electron/package.json').version")"
          ZIP="electron-v${VER}-linux-x64.zip"
          EXPECT="$(node -e "process.stdout.write((require('./node_modules/electron/checksums.json')['${ZIP}']||'').toLowerCase())")"
          echo "electron ${VER}; expected sha256=${EXPECT:-<none>}"
          for base in \
            "https://npmmirror.com/mirrors/electron/v${VER}" \
            "https://github.com/electron/electron/releases/download/v${VER}"; do
            url="${base}/${ZIP}"
            echo "::group::download ${url}"
            rm -f "/tmp/${ZIP}"
            if ! curl -fsSL --retry 5 --retry-all-errors --connect-timeout 30 -o "/tmp/${ZIP}" "${url}"; then
              echo "curl failed: ${url}"; echo "::endgroup::"; continue
            fi
            sz="$(stat -c%s "/tmp/${ZIP}")"
            echo "downloaded bytes=${sz} from ${url}"
            if [ "${sz}" -lt 50000000 ]; then
              echo "TRUNCATED (${sz} bytes < 50MB) from ${url}"; echo "::endgroup::"; continue
            fi
            actual="$(sha256sum "/tmp/${ZIP}" | cut -d' ' -f1)"
            if [ -n "${EXPECT}" ] && [ "${EXPECT}" != "${actual}" ]; then
              echo "SHA256 MISMATCH expect=${EXPECT} actual=${actual} from ${url}"; echo "::endgroup::"; continue
            fi
            echo "download verified (size+sha256); extracting"
            df -h /
            rm -rf node_modules/electron/dist
            mkdir -p node_modules/electron/dist
            if ! unzip -q "/tmp/${ZIP}" -d node_modules/electron/dist; then
              echo "UNZIP FAILED from ${url}"; df -h /; echo "::endgroup::"; continue
            fi
            chmod +x node_modules/electron/dist/electron || true
            printf 'electron' > node_modules/electron/path.txt
            if electron_ok; then
              echo "INSTALLED OK from ${url}"; echo "::endgroup::"; break
            fi
            echo "post-extract check failed from ${url}"; echo "::endgroup::"
          done
          df -h /
          ls -la node_modules/electron/dist | head -12
          electron_ok
          node -e "const p=require('electron'); require('node:fs').accessSync(p); console.log('electron binary OK:', p)"

      # Save only a COMPLETE install: the prior step asserts electron_ok and
      # fails (skipping this step) otherwise, so reaching here means the binary
      # is good — a truncated download can never poison the cache. Subsequent
      # runs then restore it from GitHub infra, immune to the egress cap.
      - name: Save Electron binary cache
        if: steps.electron-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            node_modules/electron/dist
            node_modules/electron/path.txt
          key: ${{ steps.electron-cache.outputs.cache-primary-key }}

      # This spec drives the BUILT Electron app via Playwright's _electron API
      # (electronApp.evaluate / page.evaluate); it never uses Playwright's bundled
      # chromium. `install --with-deps chromium` downloaded that unused ~170MB
      # browser, and that CDN download stalled past the 30-min job cap on every
      # run (the spec never even started). Install only the system libraries that
      # Electron's chromium needs at runtime - no browser binary download.
      - name: Install Playwright system deps (no browser download - Electron-only spec)
        run: bunx playwright install-deps chromium

      # This job does a fast UNPACKAGED electron-vite build (no electron-builder
      # packaging). The e2e fixture defaults to PACKAGED mode in CI and looks for
      # a packaged app under out/, which this job never produces. Force DEV mode
      # so the spec launches the unpackaged build via `electron .`. The security
      # properties under test (sandbox, contextIsolation, nodeIntegration:false,
      # IPC handler registration, electron version) are identical in dev and
      # packaged mode.
      # Electron is a GUI app: on the headless runner it aborts at startup with
      # "Missing X server or $DISPLAY -> The platform failed to initialize.
      # Exiting", so Playwright's _electron.launch never gets a window and times
      # out after 60s (#361, surfaced once the binary install was fixed). Run
      # under xvfb (preinstalled on ubuntu runners) to give it a virtual display.
      - name: Run security-audit e2e spec
        env:
          E2E_DEV: '1'
        run: xvfb-run -a bunx playwright test tests/e2e/specs/security-audit-verification.e2e.ts

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pr-checks-playwright-report
          path: tests/e2e/report
          if-no-files-found: ignore
          retention-days: 7

  # Job 6: Test release scripts (fast, no build required)
  release-script-test:
    name: Release Script Test
    needs: changed-files
    if: |
      needs.changed-files.outputs.docs_only != 'true' &&
      github.event.action != 'closed' &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create mock build artifacts
        shell: bash
        run: bash scripts/create-mock-release-artifacts.sh build-artifacts

      - name: Run prepare-release-assets script
        shell: bash
        run: bash scripts/prepare-release-assets.sh build-artifacts release-assets

      - name: Validate script output
        shell: bash
        run: bash scripts/verify-release-assets.sh release-assets
