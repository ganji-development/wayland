appId: com.ferroxlabs.wayland
productName: Wayland
executableName: Wayland
copyright: Copyright © 2026 Trade Canyon

protocols:
  - name: Wayland Protocol
    schemes:
      - wayland

asar:
  smartUnpack: true

directories:
  output: out
  buildResources: resources

files:
  # electron-vite build output (main process + preload + renderer)
  - out/main/**/*
  - out/preload/**/*
  - out/renderer/**/*
  # Static resources - use explicit directory inclusion to prevent resource loss
  - public/**/*
  - package.json
  # Native modules and their runtime dependencies
  - node_modules/better-sqlite3/**/*
  - node_modules/bcrypt/**/*
  - node_modules/node-pty/**/*
  - node_modules/@mapbox/**/*
  - node_modules/detect-libc/**/*
  - node_modules/prebuild-install/**/*
  - node_modules/node-gyp-build/**/*
  - node_modules/bindings/**/*
  # CLI dependencies (needed by nested jackspeak/glob)
  - node_modules/@isaacs/cliui/**/*
  - node_modules/ansi-regex/**/*
  - node_modules/ansi-styles/**/*
  - node_modules/strip-ansi/**/*
  - node_modules/wrap-ansi/**/*
  - node_modules/string-width/**/*
  - node_modules/eastasianwidth/**/*
  - node_modules/emoji-regex/**/*
  # open library - needs to be external for Windows asar compatibility
  - node_modules/open/**/*
  - node_modules/default-browser/**/*
  - node_modules/default-browser-id/**/*
  - node_modules/run-applescript/**/*
  - node_modules/bundle-name/**/*
  # tree-sitter WASM dependencies
  - node_modules/web-tree-sitter/**/*
  - node_modules/tree-sitter-bash/**/*
  # CRITICAL: Exclude ALL tree-sitter native binaries to prevent signing issues
  - '!**/node_modules/tree-sitter-*/prebuilds/**'
  - '!**/node_modules/tree-sitter-*/build/**'
  - '!**/node_modules/tree-sitter-*/src/**'
  - '!**/node_modules/tree-sitter-*/**/*.node'
  - '!**/node_modules/tree-sitter-*/**/*.obj'
  - '!**/node_modules/tree-sitter-*/**/*.o'
  - '!**/node_modules/tree-sitter-*/**/*.cc'
  - '!**/node_modules/tree-sitter-*/**/*.c'
  # Exclude dev/test files
  - '!**/node_modules/*/{CHANGELOG.md,README.md,README,readme.md,readme}'
  - '!**/node_modules/*/{test,__tests__,tests,powered-test,example,examples}'
  - '!**/node_modules/*/{*.test.js,*.spec.js,*.test.ts,*.spec.ts}'
  - '!**/node_modules/*.d.ts'
  - '!**/node_modules/.bin'
  - '!**/node_modules/*/{docs,documentation,doc}'
  # Exclude system files
  - '!**/{.DS_Store,.git,.gitignore,.gitattributes}'
  - '!**/{__pycache__,thumbs.db,.flowconfig,.idea,.vs,.nyc_output}'
  - '!**/{appveyor.yml,.travis.yml,circle.yml}'
  - '!**/{npm-debug.log,yarn.lock,.yarn-metadata.json,yarn-error.log}'
  # Exclude large packages
  - '!**/node_modules/{@img,@emnapi}/**'
  - '!**/node_modules/@img/sharp-*/**'
  - '!**/node_modules/@anthropic-ai/claude-code/vendor/**'
  - '!**/node_modules/@anthropic-ai/claude-agent-sdk/vendor/**'
  # Exclude JAR files and JNI libraries that cause notarization issues
  - '!**/*.jar'
  - '!**/*.jnilib'
  # Exclude all vendor directories to prevent unsigned binaries
  - '!**/node_modules/*/vendor/**'
  - '!**/node_modules/@*/*/vendor/**'
  # Re-include vendor dirs for native modules that need them (if any)
  - 'node_modules/better-sqlite3/vendor/**'
  - 'node_modules/bcrypt/vendor/**'
  - 'node_modules/node-pty/vendor/**'
  # Exclude vite source maps
  - '!**/*.map'
  # Exclude syntax highlighter languages (keep common ones)
  - '!**/out/**/react-syntax-highlighter_languages_highlight_*'
  - 'out/**/react-syntax-highlighter_languages_highlight_{javascript,typescript,python,java,cpp,c,html,css,json,xml,yaml,shell,bash,dockerfile,sql,markdown,go,rust,php}'
  # Ensure vendor chunks from manual splitting are included
  - 'out/renderer/**/vendor-*.js'
  # Remap viteStaticCopy output (out/main/skills/, out/main/assistant/, out/main/static/)
  # to root-level directories inside the asar, so runtime path resolution stays simple.
  - from: out/main/skills
    to: skills
  - from: out/main/assistant
    to: assistant
  - from: out/main/static
    to: static
  # Bundled business-pack extensions: read in place from inside the asar via the
  # 'bundled' scan source (#275 — no longer copied out as loose .md files that
  # tripped AV content heuristics). viteStaticCopy emits out/main/bundled-extensions.
  - from: out/main/bundled-extensions
    to: bundled-extensions

extraResources:
  - from: public
    to: .
  # Redistribution notices for bundled Apache/MIT components, including the
  # pinned native OfficeCLI runtime.
  - from: notices
    to: notices
  # Full MIT license texts for the upstreams whose code is adapted into the app.
  # Source headers cite `LICENSES/openclaw.txt` by that exact path, so it has to
  # ship for those pointers to resolve in the packaged app.
  - from: LICENSES
    to: LICENSES
  # pptx2json is vendored under src/vendor and inlined into the main bundle, where
  # the bundler drops its `//` comment header. Ship the upstream LICENSE itself
  # rather than a second copy in the repo.
  - from: src/vendor/pptx2json/LICENSE
    to: LICENSES/pptx2json.txt
  - from: resources/app.png
    to: app.png
  - from: resources/bundled-bun
    to: bundled-bun
  # wayland-core engine binary (pre-compiled per platform/arch)
  - from: resources/bundled-wayland-core
    to: bundled-wayland-core
  # Wayland Nano first-party agent binary (pre-compiled per platform/arch)
  - from: resources/bundled-wayland-nano
    to: bundled-wayland-nano
  # Native anchored Constitution filesystem helper. The exact target helper
  # and adjacent manifest are staged from source before the ASAR is compiled;
  # the independently generated authority is compiled into main-process code.
  - from: resources/bundled-constitution-fs
    to: bundled-constitution-fs
  # Native OfficeCLI v1 authoring runtime. Prepared from an exact release asset
  # and SHA-256 verified before packaging. No hosted npm fallback is bundled.
  - from: resources/bundled-officecli
    to: bundled-officecli
  # A fail-closed shadow command follows the verified OfficeCLI directory on
  # PATH. If the verified binary is absent or disappears, a user/global binary
  # can never become an implicit fallback.
  - from: resources/managed-cli-shims
    to: managed-cli-shims
  # Exact Windows NSIS extractors used only to materialize the separately
  # SHA-256-pinned Classic v0.11.8 recovery executable. Runtime verifies these
  # bytes again before use; both architectures are bundled on every target so
  # post-pack verification cannot silently skip a recovery dependency.
  - from: node_modules/7zip-bin/win/arm64/7za.exe
    to: classic-recovery-tools/win/arm64/7za.exe
  - from: node_modules/7zip-bin/win/x64/7za.exe
    to: classic-recovery-tools/win/x64/7za.exe
  # Skills library + Wayland built-in workflows: shipped as real on-disk
  # Resources so SkillLibrary (and the plain-node wayland_search_skills
  # subprocess, which cannot read inside app.asar) can read them in packaged
  # builds. #309: ship from the PACKED staging dir (.skill-pack/), produced by
  # `build:skill-pack` in the pre-build hooks. That dir contains index.json + a
  # single opaque body blob (skill-bodies.bin) + offset index — NO loose
  # SKILL.md — so AV content heuristics have no per-file markdown to scan.
  - from: .skill-pack/skills-library
    to: skills-library
  - from: .skill-pack/bundled-workflows
    to: bundled-workflows
  # models.dev enrichment snapshot - offline floor for ModelsDevClient.
  # Committed immutable offline floor; live runtime enrichment is separate.
  - from: resources/modelsdev-snapshot.json
    to: modelsdev-snapshot.json
  # Bundled voice STT model (Whisper-tiny ONNX, ~43 MB). Ships in the
  # installer so offline dictation works on a fresh install with zero
  # download. Fetched at build time by scripts/prepareVoiceModel.js.
  - from: resources/voice-models
    to: voice-models
  # WhatsApp bridge: ESM Node subprocess forked via child_process.fork.
  # Must live outside app.asar so the path on disk is real and `fork()` works.
  # postinstall installs the bridge's own node_modules/ so this copy is self-contained.
  - from: src/process/channels/whatsapp-bridge
    to: whatsapp-bridge
    filter:
      - '**/*'
      # electron-builder applies its own node_modules exclusions when the
      # dependency tree is nested below a broader extraResources root. Copy
      # it through the dedicated matcher below instead of silently producing
      # a bridge with source files but no runtime dependencies.
      - '!node_modules/**/*'
      - '!**/*.test.*'
      - '!**/*.spec.*'
  - from: src/process/channels/whatsapp-bridge/node_modules
    to: whatsapp-bridge/node_modules
    filter:
      - '**/*'
  # signal-cli runtime: native binary spawned by SignalDaemon via child_process.spawn.
  # Must live outside app.asar so the OS can execute it directly.
  # Run `node scripts/install-signal-cli.mjs` to populate bin/ before packaging.
  - from: src/process/channels/signal-cli-runtime
    to: signal-cli-runtime
    filter:
      - '**/*'
      - '!README.md'
win:
  target:
    - nsis
    #    - msi
    - zip
  icon: resources/app.ico # Use the checked-in Windows icon resource for executable metadata/icon patching
  artifactName: ${productName}-${version}-${os}-${arch}.${ext}
  # FORK PATCH 8 - Windows code signing REMOVED. Do not reinstate on merge.
  #
  # Upstream signs here via Azure Trusted Signing against the Ferrox Labs account
  # (publisherName 'Ferrox Labs, LLC', codeSigningAccountName ferrox-labs-signing),
  # authenticating through the Azure EnvironmentCredential from AZURE_TENANT_ID /
  # AZURE_CLIENT_ID / AZURE_CLIENT_SECRET. Those are CI secrets injected only for
  # the CI Windows build. A fork build has neither the credentials nor any business
  # signing as Ferrox Labs, so the step can only fail - or stall while
  # electron-builder installs the TrustedSigning PowerShell module.
  #
  # `verifyUpdateCodeSignature` goes with it: left true, the Windows auto-updater
  # rejects this build's own unsigned Authenticode on every update check.
  #
  # Cost of this patch: SmartScreen warns on first run of the unsigned installer.
  # See FORK-PATCHES.md; guarded by forkPatchGuards.test.ts because a merge that
  # restores these keys puts the build straight back into a doomed signing attempt.
  verifyUpdateCodeSignature: false
  # Do not re-sign the third-party runtimes we bundle. Authenticode signing
  # rewrites a binary, which destroys the exact upstream bytes that
  # scripts/verify-packaged-resources.js checks against their pinned release
  # digests - the 0.12.0 Windows builds failed on precisely that, with
  # bundled-wayland-core, bundled-wayland-nano, bundled-officecli, bundled-bun
  # and whatsapp-bridge all reported as invalid after signing.
  #
  # Signing them is also wrong on its own terms: an Authenticode signature says
  # Ferrox Labs produced this binary, and bun, OfficeCLI, Core and Nano are not
  # ours to vouch for. We pin and verify their published bytes instead. macOS has
  # always done this through mac.signIgnore above; Windows has no path filter, so
  # the same exclusions are expressed as negative signExts patterns, which
  # electron-builder matches with endsWith against the full path.
  #
  # Everything else still signs exactly as before: Wayland.exe, the installer and
  # uninstaller, elevate.exe, the node-pty helpers and 7za.
  #
  # The whatsapp-bridge entries are npm shim executables created by its frozen
  # lockfile. If that lockfile ever grows a new shim, the build fails closed on
  # the packaged-resource check rather than shipping a mismatch, and this list
  # needs the new name.
  signExts:
    - '!\bundled-bun\win32-x64\bun.exe'
    - '!\bundled-bun\win32-arm64\bun.exe'
    - '!\bundled-officecli\win32-x64\officecli.exe'
    - '!\bundled-officecli\win32-arm64\officecli.exe'
    - '!\bundled-wayland-core\win32-x64\wayland-core.exe'
    - '!\bundled-wayland-core\win32-arm64\wayland-core.exe'
    - '!\bundled-wayland-nano\win32-x64\wayland-nano.exe'
    - '!\bundled-wayland-nano\win32-arm64\wayland-nano.exe'
    - '!\whatsapp-bridge\node_modules\.bin\browsers.exe'
    - '!\whatsapp-bridge\node_modules\.bin\crc32.exe'
    - '!\whatsapp-bridge\node_modules\.bin\escodegen.exe'
    - '!\whatsapp-bridge\node_modules\.bin\esgenerate.exe'
    - '!\whatsapp-bridge\node_modules\.bin\esparse.exe'
    - '!\whatsapp-bridge\node_modules\.bin\esvalidate.exe'
    - '!\whatsapp-bridge\node_modules\.bin\extract-zip.exe'
    - '!\whatsapp-bridge\node_modules\.bin\glob.exe'
    - '!\whatsapp-bridge\node_modules\.bin\js-yaml.exe'
    - '!\whatsapp-bridge\node_modules\.bin\mime.exe'
    - '!\whatsapp-bridge\node_modules\.bin\pino.exe'
    - '!\whatsapp-bridge\node_modules\.bin\puppeteer.exe'
    - '!\whatsapp-bridge\node_modules\.bin\qrcode-terminal.exe'
    - '!\whatsapp-bridge\node_modules\.bin\semver.exe'
    - '!\whatsapp-bridge\node_modules\.bin\which.exe'
    - '!\whatsapp-bridge\node_modules\cross-spawn\node_modules\.bin\node-which.exe'

nsis:
  oneClick: false
  # UPD-04: install per-machine into a root/admin-writable Program Files
  # location so the bundled engine binary cannot be swapped by an unprivileged
  # process between launches (tamper-then-spawn). Tradeoff recorded in residual:
  # the installer now elevates (UAC) and installs for all users instead of the
  # current user only.
  perMachine: true
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  # #138: wipe the app's per-user data (config, cache, DB) on uninstall so a later
  # reinstall starts clean instead of a stale DB shadowing the new build. Scope is
  # the Electron userData dirs only; home dotfiles (~/.wayland, ~/.codex, ~/.grok)
  # are user-owned and intentionally left in place.
  deleteAppDataOnUninstall: true
  artifactName: ${productName}-${version}-${os}-${arch}.${ext}
  # #715: emit a .blockmap next to the installer so electron-updater can do
  # differential downloads instead of always falling back to the full package.
  differentialPackage: true

mac:
  target:
    - dmg
    - zip
  icon: resources/app.icns
  artifactName: ${productName}-${version}-${os}-${arch}.${ext}
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: entitlements.plist
  entitlementsInherit: entitlements.plist
  # Preserve the exact independently pinned nested executable bytes. OfficeCLI
  # is already Developer ID signed by its publisher. The released wayland-core
  # and wayland-nano binaries are linker-signed. Re-signing any of them with
  # Electron helper entitlements changes its pinned digest and grants a much
  # broader authority surface than the standalone tool was built with; the
  # post-package gate rechecks the exact bytes from Contents/Resources.
  signIgnore:
    - '/Contents/Resources/bundled-bun/[^/]+/bun$'
    - '/Contents/Resources/whatsapp-bridge/node_modules/@img/sharp-(?:libvips-)?darwin-(?:arm64|x64)/.*(?:\.node|\.dylib)$'
    - '/Contents/Resources/whatsapp-bridge/node_modules/(?:bare-fs|bare-os|bare-url)/prebuilds/(?:darwin-(?:arm64|x64)|ios-(?:arm64|arm64-simulator|x64-simulator))/[^/]+\.bare$'
    - '/Contents/Resources/bundled-officecli/[^/]+/officecli$'
    - '/Contents/Resources/bundled-wayland-core/[^/]+/wayland-core$'
    - '/Contents/Resources/bundled-wayland-nano/[^/]+/wayland-nano$'
    - '/Contents/Resources/bundled-constitution-fs/[^/]+/wayland-constitution-fs$'
  # #466: Computer-Use uses Screen Recording (screenshots) + Apple Events (to
  # control other apps). NSAppleEventsUsageDescription IS an Apple-honored TCC
  # purpose string - macOS shows it on the automation consent prompt.
  # NSScreenCaptureUsageDescription is NOT an Apple-documented key: macOS
  # generates the Screen Recording prompt itself and does not surface a custom
  # string, so this is harmless documentation only (some tooling looks for it),
  # NOT a hard requirement. Accessibility has no Info.plist key at all - it is
  # granted via the AX API, and the engine owns that prompt (#114).
  extendInfo:
    NSScreenCaptureUsageDescription: >-
      Wayland captures your screen so the Computer-Use agent can see what is on
      it and act on your behalf. Nothing is captured unless you start a
      Computer-Use task.
    NSAppleEventsUsageDescription: >-
      Wayland controls other apps on your behalf when you ask the Computer-Use
      agent to complete a task.
    # NSMicrophoneUsageDescription IS an Apple-honored TCC purpose string: macOS
    # surfaces it on the microphone consent prompt. Required (with the
    # com.apple.security.device.audio-input entitlement) for voice input
    # (speech-to-text) to capture the mic on a hardened, signed build.
    NSMicrophoneUsageDescription: >-
      Wayland uses your microphone for voice input (speech-to-text) in the chat
      box. Audio is only captured while you are dictating.

dmg:
  # Use UDZO format which is more reliable on CI
  format: UDZO
  # Disable internet-enabled DMG to avoid network issues
  internetEnabled: false
  # Simple window settings to avoid layout issues
  window:
    width: 540
    height: 380
  # Contents positioning
  contents:
    - x: 140
      y: 180
      type: file
    - x: 400
      y: 180
      type: link
      path: /Applications

afterPack: scripts/afterPack.js
afterSign: scripts/afterSign.js
# afterSign staples the .app; this staples the .dmg (a separate ticket) so a
# browser-downloaded disk image isn't rejected by Gatekeeper as "damaged".
afterAllArtifactBuild: scripts/notarizeDmg.js

linux:
  # XP-BUILD-LINUX-01: ship distro-agnostic AppImage + rpm alongside deb so
  # Fedora/RHEL/openSUSE and non-Debian users get an installable artifact.
  target:
    - AppImage
    - deb
    - rpm
  icon: resources/app.png
  artifactName: ${productName}-${version}-${os}-${arch}.${ext}
  category: Utility
  maintainer: wayland
  vendor: wayland
  synopsis: ${description}
  description: ${description}
  desktop:
    entry:
      Name: Wayland
      Comment: ${description}
      Icon: wayland
      Categories: Office;Utility;
      MimeType: x-scheme-handler/wayland;

npmRebuild: false
buildDependenciesFromSource: false
nodeGypRebuild: false

asarUnpack:
  - '**/node_modules/better-sqlite3/**/*'
  - '**/node_modules/bcrypt/**/*'
  - '**/node_modules/node-pty/**/*'
  - '**/node_modules/@mapbox/**/*'
  - '**/node_modules/detect-libc/**/*'
  - '**/node_modules/prebuild-install/**/*'
  - '**/node_modules/node-gyp-build/**/*'
  - '**/node_modules/bindings/**/*'
  # open library and dependencies - needed for Windows asar compatibility
  - '**/node_modules/open/**/*'
  - '**/node_modules/default-browser/**/*'
  - '**/node_modules/default-browser-id/**/*'
  - '**/node_modules/run-applescript/**/*'
  - '**/node_modules/bundle-name/**/*'
  # tree-sitter WASM files need to be unpacked for fs.readFile access
  - '**/node_modules/web-tree-sitter/**/*'
  - '**/node_modules/tree-sitter-bash/**/*'
  # skills/ and assistant/ no longer need asarUnpack:
  # initStorage copies them from asar to config/builtin-skills/ and config/assistants/
  # at startup, so all runtime reads go through the user directory, not asar.
  # Builtin MCP server scripts must be unpacked so external node processes can execute them
  - 'out/main/builtin-mcp-image-gen.js'
  - 'out/main/builtin-mcp-search-skills.js'
  - 'out/main/builtin-mcp-concierge-diag.js'
  - 'out/main/team-mcp-stdio.js'
  - 'out/main/team-guide-mcp-stdio.js'
  # Bundled @wayland MCP servers (ship with installer, no npm publish)
  - 'out/main/builtin-mcp-apple.mjs'
  - 'out/main/builtin-mcp-imap.mjs'
  - 'out/main/builtin-mcp-news.mjs'
  - 'out/main/builtin-mcp-cal-com.mjs'
  # Apple MCP native EventKit bridge (macOS only - built by `swiftc`)
  - 'out/main/eventkit-bridge'
  # NOTE: skills-library + bundled-workflows now ship via extraResources
  # (Contents/Resources/{skills-library,bundled-workflows}); the old asarUnpack
  # entries here matched nothing (source is under src/process/resources, never
  # in the asar) so they were removed.
# Compression level: normal for faster builds, maximum for smallest size
# Note: electron-builder doesn't support YAML template syntax for env vars
# The build script sets ELECTRON_BUILDER_COMPRESSION_LEVEL env var which takes precedence
compression: normal
removePackageScripts: true
electronDownload:
  cache: ${env.ELECTRON_CACHE}

publish:
  provider: github
  owner: FerroxLabs
  repo: wayland
  publishAutoUpdate: true
  releaseType: release
